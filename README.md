# GZ GraphQL - Анализ государственных закупок Казахстана

Инструменты для работы с API портала государственных закупок (goszakup.gov.kz) через GraphQL v3.

## 📁 Структура проекта

- `src/` — Исходный код и конфигурация.
- `reports/` — Автоматически создаваемая папка для Excel-отчетов.
- `.env` — Файл для хранения токена и БИН (создается вручную из примера).

## 🚀 Установка

1. Клонируйте репозиторий.
2. Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv venv
source venv/bin/activate  # Для Linux/macOS
# или
.\venv\Scripts\activate   # Для Windows

pip install -r requirements.txt
```

## ⚙️ Настройка

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```
2. Откройте `.env` и заполните данные:
   - `TOKEN` — Ваш API-токен с портала Goszakup.
   - `BIN_COMPANY` — БИН организации заказчика.
   - `FIN_YEAR` — Финансовый год для отчета.

## 📊 Запуск скриптов

Все скрипты запускаются из корневой папки проекта:

### 1. Получение списка всех договоров
```bash
python src/get_contracts.py
```
**Результат:** Excel-файл со списком договоров в папке `reports/`.

### 2. Генерация аналитического отчета
```bash
python src/generate_report.py
```
**Результат:** Подробный аналитический отчет (Таблицы 1 и 2, экономия, расторгнутые договоры) в папке `reports/`.

### 3. Проверка объявлений
```bash
python src/get_announcements.py
```
**Результат:** Статистика по опубликованным объявлениям в консоли.

## 📝 Особенности расчета
- **Экономия:** Рассчитывается как `Плановая сумма - Фактическая сумма`. Если фактическая сумма не указана или равна 0, используется сумма договора.
- **Статусы:** В аналитический отчет по умолчанию включены статусы: *Исполнен (390)*, *Частично исполнен (375)* и *Действует (190)*.

## 🛡️ Безопасность
Файл `.env` добавлен в `.gitignore` и не попадает в репозиторий. Никогда не публикуйте свои токены в открытом доступе.
